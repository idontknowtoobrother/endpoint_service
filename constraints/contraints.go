package constraints

var (
	ErrInvalidQuery           = "invalid query, needs: %s"
	ErrEndpointHasBeenDeleted = "endpoint has been deleted, reason: %s"
	ErrPathIsRequired         = "path is required"
	ErrNotFound               = "not found"
)
